package com.distsystem.interfaces;

/** interface for Remote manager able to define local objects and remove call on them among Agents.
 * Each object is registered locally and that information is distributed to other Agents so object can be called.
 * */
public interface Remote extends DistService {
    
}
