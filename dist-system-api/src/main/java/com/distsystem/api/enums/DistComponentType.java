package com.distsystem.api.enums;

/** components in Agent */
public enum DistComponentType {
    agent,
    issues,
    events,
    connectors,
    registrations,
    registration,
    services,
    timers,
    threads,
    api,
    cache,
    client,
    server,
    daos,
    dao,
    receiver;
}
